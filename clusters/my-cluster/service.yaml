apiVersion: v1
kind: Service
metadata:
  name: test-app
  namespace: default
  labels:
    app.kubernetes.io/name: test-app
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: test-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
